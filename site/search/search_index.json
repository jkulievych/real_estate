{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Estymator Cen Nieruchomo\u015bci","text":"<p>Aplikacja do szacowania cen mieszka\u0144 na podstawie danych z rumu\u0144skiego portalu imobiliare.ro.</p>"},{"location":"#opis-projektu","title":"Opis projektu","text":"<p>Projekt sk\u0142ada si\u0119 z czterech g\u0142\u00f3wnych modu\u0142\u00f3w:</p> <ul> <li>scraper.py \u2014 pobiera og\u0142oszenia nieruchomo\u015bci z imobiliare.ro</li> <li>features.py \u2014 wyci\u0105ga dodatkowe cechy z opis\u00f3w og\u0142osze\u0144 (parking, balkon, metro itp.)</li> <li>model.py \u2014 trenuje modele regresji liniowej i lasu losowego do przewidywania cen</li> <li>app.py \u2014 aplikacja GUI (Flet) do szacowania cen przez u\u017cytkownika</li> </ul>"},{"location":"#dane","title":"Dane","text":"<p>Zebrano ponad 7000 og\u0142osze\u0144 mieszka\u0144 na sprzeda\u017c w Rumunii. Po filtracji (tylko rekordy z podan\u0105 powierzchni\u0105, ceny 10 000\u20132 000 000 EUR) zbi\u00f3r liczy ~7000 rekord\u00f3w.</p>"},{"location":"#modele","title":"Modele","text":"Model R\u00b2 RMSE MAE Regresja liniowa 0.44 115 408 EUR 63 852 EUR Las losowy 0.56 102 423 EUR 50 753 EUR"},{"location":"#instalacja","title":"Instalacja","text":"<pre><code>pip install -r requirements.txt\npython features.py   # ekstrakcja cech\npython model.py      # trening modeli\npython app.py        # uruchomienie aplikacji\n</code></pre>"},{"location":"#testy","title":"Testy","text":"<pre><code>python -m pytest tests/ -v\n</code></pre>"},{"location":"#autor","title":"Autor","text":"<p>Yuliia Kuliievych \u2014 projekt zaliczeniowy</p>"},{"location":"#licencja","title":"Licencja","text":"<p>MIT</p>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#scraperpy","title":"scraper.py","text":"<p>Author: Yuliia Kuliievych License: MIT</p>"},{"location":"api/#featurespy","title":"features.py","text":"<p>features.py - Ekstrakcja cech ze s\u0142ownika opis\u00f3w og\u0142osze\u0144 nieruchomo\u015bci.</p> <p>Author: Yuliia Kuliievych License: MIT</p>"},{"location":"api/#features.FeatureExtractor","title":"<code>FeatureExtractor</code>","text":"<p>Klasa do ekstrakcji cech binarnych z opis\u00f3w og\u0142osze\u0144.</p> Source code in <code>features.py</code> <pre><code>class FeatureExtractor:\n    \"\"\"Klasa do ekstrakcji cech binarnych z opis\u00f3w og\u0142osze\u0144.\"\"\"\n\n    def __init__(self, keywords: dict = KEYWORDS):\n        \"\"\"\n        Inicjalizacja ekstraktora cech.\n\n        Args:\n            keywords: S\u0142ownik {nazwa_cechy: wzorzec_regex}.\n        \"\"\"\n        self.keywords = keywords\n\n    def extract(self, description: str) -&gt; dict:\n        \"\"\"\n        Wyci\u0105ga cechy binarne z opisu og\u0142oszenia.\n\n        Args:\n            description: Tekst opisu og\u0142oszenia.\n\n        Returns:\n            S\u0142ownik {nazwa_cechy: 0 lub 1}.\n        \"\"\"\n        result = {}\n        for feature, pattern in self.keywords.items():\n            result[feature] = 1 if re.search(pattern, description, re.I) else 0\n        return result\n\n    def extract_desc_length(self, description: str) -&gt; int:\n        \"\"\"\n        Zwraca d\u0142ugo\u015b\u0107 opisu w znakach.\n\n        Args:\n            description: Tekst opisu og\u0142oszenia.\n\n        Returns:\n            Liczba znak\u00f3w w opisie.\n        \"\"\"\n        return len(description.strip())\n\n    def process_file(self, input_path: str, output_path: str) -&gt; None:\n        \"\"\"\n        Przetwarza plik CSV \u2014 dodaje kolumny z cechami i zapisuje wynik.\n\n        Args:\n            input_path: \u015acie\u017cka do pliku wej\u015bciowego CSV.\n            output_path: \u015acie\u017cka do pliku wyj\u015bciowego CSV.\n        \"\"\"\n        rows = list(csv.DictReader(open(input_path, encoding=\"utf-8-sig\")))\n\n        new_fields = list(self.keywords.keys()) + [\"desc_length\"]\n        fields = list(rows[0].keys()) + new_fields\n\n        for r in rows:\n            desc = r.get(\"description\", \"\")\n            features = self.extract(desc)\n            features[\"desc_length\"] = self.extract_desc_length(desc)\n            r.update(features)\n\n        with open(output_path, \"w\", newline=\"\", encoding=\"utf-8-sig\") as f:\n            writer = csv.DictWriter(f, fieldnames=fields)\n            writer.writeheader()\n            writer.writerows(rows)\n\n        counts = {f: sum(r[f] == \"1\" or r[f] == 1 for r in rows) for f in self.keywords}\n        print(f\"Przetworzono {len(rows)} rekord\u00f3w \u2192 '{output_path}'\")\n        print(\"Cz\u0119sto\u015b\u0107 cech:\")\n        for feat, count in counts.items():\n            print(f\"  {feat:25}: {count:4} ({count / len(rows) * 100:.0f}%)\")\n</code></pre>"},{"location":"api/#features.FeatureExtractor.__init__","title":"<code>__init__(keywords=KEYWORDS)</code>","text":"<p>Inicjalizacja ekstraktora cech.</p> <p>Parameters:</p> Name Type Description Default <code>keywords</code> <code>dict</code> <p>S\u0142ownik {nazwa_cechy: wzorzec_regex}.</p> <code>KEYWORDS</code> Source code in <code>features.py</code> <pre><code>def __init__(self, keywords: dict = KEYWORDS):\n    \"\"\"\n    Inicjalizacja ekstraktora cech.\n\n    Args:\n        keywords: S\u0142ownik {nazwa_cechy: wzorzec_regex}.\n    \"\"\"\n    self.keywords = keywords\n</code></pre>"},{"location":"api/#features.FeatureExtractor.extract","title":"<code>extract(description)</code>","text":"<p>Wyci\u0105ga cechy binarne z opisu og\u0142oszenia.</p> <p>Parameters:</p> Name Type Description Default <code>description</code> <code>str</code> <p>Tekst opisu og\u0142oszenia.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>S\u0142ownik {nazwa_cechy: 0 lub 1}.</p> Source code in <code>features.py</code> <pre><code>def extract(self, description: str) -&gt; dict:\n    \"\"\"\n    Wyci\u0105ga cechy binarne z opisu og\u0142oszenia.\n\n    Args:\n        description: Tekst opisu og\u0142oszenia.\n\n    Returns:\n        S\u0142ownik {nazwa_cechy: 0 lub 1}.\n    \"\"\"\n    result = {}\n    for feature, pattern in self.keywords.items():\n        result[feature] = 1 if re.search(pattern, description, re.I) else 0\n    return result\n</code></pre>"},{"location":"api/#features.FeatureExtractor.extract_desc_length","title":"<code>extract_desc_length(description)</code>","text":"<p>Zwraca d\u0142ugo\u015b\u0107 opisu w znakach.</p> <p>Parameters:</p> Name Type Description Default <code>description</code> <code>str</code> <p>Tekst opisu og\u0142oszenia.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Liczba znak\u00f3w w opisie.</p> Source code in <code>features.py</code> <pre><code>def extract_desc_length(self, description: str) -&gt; int:\n    \"\"\"\n    Zwraca d\u0142ugo\u015b\u0107 opisu w znakach.\n\n    Args:\n        description: Tekst opisu og\u0142oszenia.\n\n    Returns:\n        Liczba znak\u00f3w w opisie.\n    \"\"\"\n    return len(description.strip())\n</code></pre>"},{"location":"api/#features.FeatureExtractor.process_file","title":"<code>process_file(input_path, output_path)</code>","text":"<p>Przetwarza plik CSV \u2014 dodaje kolumny z cechami i zapisuje wynik.</p> <p>Parameters:</p> Name Type Description Default <code>input_path</code> <code>str</code> <p>\u015acie\u017cka do pliku wej\u015bciowego CSV.</p> required <code>output_path</code> <code>str</code> <p>\u015acie\u017cka do pliku wyj\u015bciowego CSV.</p> required Source code in <code>features.py</code> <pre><code>def process_file(self, input_path: str, output_path: str) -&gt; None:\n    \"\"\"\n    Przetwarza plik CSV \u2014 dodaje kolumny z cechami i zapisuje wynik.\n\n    Args:\n        input_path: \u015acie\u017cka do pliku wej\u015bciowego CSV.\n        output_path: \u015acie\u017cka do pliku wyj\u015bciowego CSV.\n    \"\"\"\n    rows = list(csv.DictReader(open(input_path, encoding=\"utf-8-sig\")))\n\n    new_fields = list(self.keywords.keys()) + [\"desc_length\"]\n    fields = list(rows[0].keys()) + new_fields\n\n    for r in rows:\n        desc = r.get(\"description\", \"\")\n        features = self.extract(desc)\n        features[\"desc_length\"] = self.extract_desc_length(desc)\n        r.update(features)\n\n    with open(output_path, \"w\", newline=\"\", encoding=\"utf-8-sig\") as f:\n        writer = csv.DictWriter(f, fieldnames=fields)\n        writer.writeheader()\n        writer.writerows(rows)\n\n    counts = {f: sum(r[f] == \"1\" or r[f] == 1 for r in rows) for f in self.keywords}\n    print(f\"Przetworzono {len(rows)} rekord\u00f3w \u2192 '{output_path}'\")\n    print(\"Cz\u0119sto\u015b\u0107 cech:\")\n    for feat, count in counts.items():\n        print(f\"  {feat:25}: {count:4} ({count / len(rows) * 100:.0f}%)\")\n</code></pre>"},{"location":"api/#modelpy","title":"model.py","text":"<p>model.py - Budowa i zapis modeli regresji ceny nieruchomo\u015bci.</p> Modele <ul> <li>Regresja liniowa (LinearRegression)</li> <li>Las losowy (RandomForestRegressor)</li> </ul> <p>Dane wej\u015bciowe:  data/processed/imobiliare.csv Dane wyj\u015bciowe:  models/linear_model.pkl                  models/rf_model.pkl                  models/metadata.json</p> <p>Author: Yuliia Kuliievych License: MIT</p>"},{"location":"api/#model.DataLoader","title":"<code>DataLoader</code>","text":"<p>Klasa do \u0142adowania i czyszczenia danych z pliku CSV.</p> Source code in <code>model.py</code> <pre><code>class DataLoader:\n    \"\"\"Klasa do \u0142adowania i czyszczenia danych z pliku CSV.\"\"\"\n\n    def __init__(self, filepath: str):\n        \"\"\"\n        Args:\n            filepath: \u015acie\u017cka do pliku CSV.\n        \"\"\"\n        self.filepath = filepath\n\n    def load(self) -&gt; tuple[list, list]:\n        \"\"\"\n        \u0141aduje dane, usuwa outliery i zwraca X, y.\n\n        Returns:\n            Tuple (X, y) gdzie X to lista wektor\u00f3w cech, y to lista cen.\n        \"\"\"\n        rows = list(csv.DictReader(open(self.filepath, encoding=\"utf-8-sig\")))\n\n        X, y = [], []\n        skipped = 0\n        for r in rows:\n            try:\n                price = float(r[TARGET])\n                area  = float(r[\"area_m2\"])\n                if not (PRICE_MIN &lt;= price &lt;= PRICE_MAX):\n                    skipped += 1\n                    continue\n                if not (AREA_MIN &lt;= area &lt;= AREA_MAX):\n                    skipped += 1\n                    continue\n                x_row = [float(r[f]) for f in FEATURES]\n                X.append(x_row)\n                y.append(price)\n            except (ValueError, KeyError):\n                skipped += 1\n\n        print(f\"Za\u0142adowano {len(X)} rekord\u00f3w, pomini\u0119to {skipped}\")\n        return X, y\n\n    def train_test_split(self, X: list, y: list,\n                         test_size: float = 0.2,\n                         seed: int = 42) -&gt; tuple:\n        \"\"\"\n        Dzieli dane na zbi\u00f3r treningowy i testowy.\n\n        Args:\n            X: Lista wektor\u00f3w cech.\n            y: Lista warto\u015bci docelowych.\n            test_size: Udzia\u0142 zbioru testowego (0-1).\n            seed: Ziarno losowo\u015bci.\n\n        Returns:\n            Tuple (X_train, X_test, y_train, y_test).\n        \"\"\"\n        import random\n        random.seed(seed)\n        indices = list(range(len(X)))\n        random.shuffle(indices)\n        split = int(len(X) * (1 - test_size))\n        train_idx = indices[:split]\n        test_idx  = indices[split:]\n        X_train = [X[i] for i in train_idx]\n        X_test  = [X[i] for i in test_idx]\n        y_train = [y[i] for i in train_idx]\n        y_test  = [y[i] for i in test_idx]\n        return X_train, X_test, y_train, y_test\n</code></pre>"},{"location":"api/#model.DataLoader.__init__","title":"<code>__init__(filepath)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>\u015acie\u017cka do pliku CSV.</p> required Source code in <code>model.py</code> <pre><code>def __init__(self, filepath: str):\n    \"\"\"\n    Args:\n        filepath: \u015acie\u017cka do pliku CSV.\n    \"\"\"\n    self.filepath = filepath\n</code></pre>"},{"location":"api/#model.DataLoader.load","title":"<code>load()</code>","text":"<p>\u0141aduje dane, usuwa outliery i zwraca X, y.</p> <p>Returns:</p> Type Description <code>tuple[list, list]</code> <p>Tuple (X, y) gdzie X to lista wektor\u00f3w cech, y to lista cen.</p> Source code in <code>model.py</code> <pre><code>def load(self) -&gt; tuple[list, list]:\n    \"\"\"\n    \u0141aduje dane, usuwa outliery i zwraca X, y.\n\n    Returns:\n        Tuple (X, y) gdzie X to lista wektor\u00f3w cech, y to lista cen.\n    \"\"\"\n    rows = list(csv.DictReader(open(self.filepath, encoding=\"utf-8-sig\")))\n\n    X, y = [], []\n    skipped = 0\n    for r in rows:\n        try:\n            price = float(r[TARGET])\n            area  = float(r[\"area_m2\"])\n            if not (PRICE_MIN &lt;= price &lt;= PRICE_MAX):\n                skipped += 1\n                continue\n            if not (AREA_MIN &lt;= area &lt;= AREA_MAX):\n                skipped += 1\n                continue\n            x_row = [float(r[f]) for f in FEATURES]\n            X.append(x_row)\n            y.append(price)\n        except (ValueError, KeyError):\n            skipped += 1\n\n    print(f\"Za\u0142adowano {len(X)} rekord\u00f3w, pomini\u0119to {skipped}\")\n    return X, y\n</code></pre>"},{"location":"api/#model.DataLoader.train_test_split","title":"<code>train_test_split(X, y, test_size=0.2, seed=42)</code>","text":"<p>Dzieli dane na zbi\u00f3r treningowy i testowy.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>list</code> <p>Lista wektor\u00f3w cech.</p> required <code>y</code> <code>list</code> <p>Lista warto\u015bci docelowych.</p> required <code>test_size</code> <code>float</code> <p>Udzia\u0142 zbioru testowego (0-1).</p> <code>0.2</code> <code>seed</code> <code>int</code> <p>Ziarno losowo\u015bci.</p> <code>42</code> <p>Returns:</p> Type Description <code>tuple</code> <p>Tuple (X_train, X_test, y_train, y_test).</p> Source code in <code>model.py</code> <pre><code>def train_test_split(self, X: list, y: list,\n                     test_size: float = 0.2,\n                     seed: int = 42) -&gt; tuple:\n    \"\"\"\n    Dzieli dane na zbi\u00f3r treningowy i testowy.\n\n    Args:\n        X: Lista wektor\u00f3w cech.\n        y: Lista warto\u015bci docelowych.\n        test_size: Udzia\u0142 zbioru testowego (0-1).\n        seed: Ziarno losowo\u015bci.\n\n    Returns:\n        Tuple (X_train, X_test, y_train, y_test).\n    \"\"\"\n    import random\n    random.seed(seed)\n    indices = list(range(len(X)))\n    random.shuffle(indices)\n    split = int(len(X) * (1 - test_size))\n    train_idx = indices[:split]\n    test_idx  = indices[split:]\n    X_train = [X[i] for i in train_idx]\n    X_test  = [X[i] for i in test_idx]\n    y_train = [y[i] for i in train_idx]\n    y_test  = [y[i] for i in test_idx]\n    return X_train, X_test, y_train, y_test\n</code></pre>"},{"location":"api/#model.LinearRegressionModel","title":"<code>LinearRegressionModel</code>","text":"<p>Regresja liniowa metod\u0105 najmniejszych kwadrat\u00f3w (OLS). Implementacja bez zewn\u0119trznych bibliotek ML.</p> Source code in <code>model.py</code> <pre><code>class LinearRegressionModel:\n    \"\"\"\n    Regresja liniowa metod\u0105 najmniejszych kwadrat\u00f3w (OLS).\n    Implementacja bez zewn\u0119trznych bibliotek ML.\n    \"\"\"\n\n    def __init__(self):\n        \"\"\"Inicjalizacja modelu.\"\"\"\n        self.coefficients = []\n        self.intercept    = 0.0\n        self.feature_names = FEATURES\n\n    def _transpose(self, matrix: list) -&gt; list:\n        return [[row[i] for row in matrix] for i in range(len(matrix[0]))]\n\n    def _matmul(self, A: list, B: list) -&gt; list:\n        result = [[0.0] * len(B[0]) for _ in range(len(A))]\n        for i in range(len(A)):\n            for j in range(len(B[0])):\n                for k in range(len(B)):\n                    result[i][j] += A[i][k] * B[k][j]\n        return result\n\n    def _inverse(self, matrix: list) -&gt; list:\n        \"\"\"Odwr\u00f3cenie macierzy metod\u0105 Gaussa-Jordana.\"\"\"\n        n = len(matrix)\n        aug = [row[:] + [1.0 if i == j else 0.0 for j in range(n)]\n               for i, row in enumerate(matrix)]\n        for col in range(n):\n            pivot = max(range(col, n), key=lambda r: abs(aug[r][col]))\n            aug[col], aug[pivot] = aug[pivot], aug[col]\n            div = aug[col][col]\n            if abs(div) &lt; 1e-12:\n                raise ValueError(\"Macierz osobliwa \u2014 nie mo\u017cna odwr\u00f3ci\u0107.\")\n            aug[col] = [x / div for x in aug[col]]\n            for row in range(n):\n                if row != col:\n                    factor = aug[row][col]\n                    aug[row] = [aug[row][k] - factor * aug[col][k]\n                                for k in range(2 * n)]\n        return [row[n:] for row in aug]\n\n    def fit(self, X: list, y: list) -&gt; None:\n        \"\"\"\n        Dopasowuje model do danych treningowych (OLS).\n\n        Args:\n            X: Lista wektor\u00f3w cech.\n            y: Lista warto\u015bci docelowych.\n        \"\"\"\n        # Dodaj kolumn\u0119 jedynek (intercept)\n        X_b = [[1.0] + row for row in X]\n        Xt   = self._transpose(X_b)\n        XtX  = self._matmul(Xt, X_b)\n        Xty  = [[sum(Xt[i][k] * y[k] for k in range(len(y)))]\n                for i in range(len(Xt))]\n        inv  = self._inverse(XtX)\n        beta = self._matmul(inv, Xty)\n        self.intercept    = beta[0][0]\n        self.coefficients = [beta[i + 1][0] for i in range(len(FEATURES))]\n\n    def predict(self, X: list) -&gt; list:\n        \"\"\"\n        Przewiduje ceny dla podanych wektor\u00f3w cech.\n\n        Args:\n            X: Lista wektor\u00f3w cech.\n\n        Returns:\n            Lista przewidywanych cen.\n        \"\"\"\n        preds = []\n        for row in X:\n            val = self.intercept + sum(c * x for c, x in\n                                       zip(self.coefficients, row))\n            preds.append(max(val, 0.0))\n        return preds\n\n    def predict_one(self, x: list) -&gt; float:\n        \"\"\"\n        Przewiduje cen\u0119 dla jednego wektora cech.\n\n        Args:\n            x: Wektor cech.\n\n        Returns:\n            Przewidywana cena.\n        \"\"\"\n        return self.predict([x])[0]\n</code></pre>"},{"location":"api/#model.LinearRegressionModel.__init__","title":"<code>__init__()</code>","text":"<p>Inicjalizacja modelu.</p> Source code in <code>model.py</code> <pre><code>def __init__(self):\n    \"\"\"Inicjalizacja modelu.\"\"\"\n    self.coefficients = []\n    self.intercept    = 0.0\n    self.feature_names = FEATURES\n</code></pre>"},{"location":"api/#model.LinearRegressionModel.fit","title":"<code>fit(X, y)</code>","text":"<p>Dopasowuje model do danych treningowych (OLS).</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>list</code> <p>Lista wektor\u00f3w cech.</p> required <code>y</code> <code>list</code> <p>Lista warto\u015bci docelowych.</p> required Source code in <code>model.py</code> <pre><code>def fit(self, X: list, y: list) -&gt; None:\n    \"\"\"\n    Dopasowuje model do danych treningowych (OLS).\n\n    Args:\n        X: Lista wektor\u00f3w cech.\n        y: Lista warto\u015bci docelowych.\n    \"\"\"\n    # Dodaj kolumn\u0119 jedynek (intercept)\n    X_b = [[1.0] + row for row in X]\n    Xt   = self._transpose(X_b)\n    XtX  = self._matmul(Xt, X_b)\n    Xty  = [[sum(Xt[i][k] * y[k] for k in range(len(y)))]\n            for i in range(len(Xt))]\n    inv  = self._inverse(XtX)\n    beta = self._matmul(inv, Xty)\n    self.intercept    = beta[0][0]\n    self.coefficients = [beta[i + 1][0] for i in range(len(FEATURES))]\n</code></pre>"},{"location":"api/#model.LinearRegressionModel.predict","title":"<code>predict(X)</code>","text":"<p>Przewiduje ceny dla podanych wektor\u00f3w cech.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>list</code> <p>Lista wektor\u00f3w cech.</p> required <p>Returns:</p> Type Description <code>list</code> <p>Lista przewidywanych cen.</p> Source code in <code>model.py</code> <pre><code>def predict(self, X: list) -&gt; list:\n    \"\"\"\n    Przewiduje ceny dla podanych wektor\u00f3w cech.\n\n    Args:\n        X: Lista wektor\u00f3w cech.\n\n    Returns:\n        Lista przewidywanych cen.\n    \"\"\"\n    preds = []\n    for row in X:\n        val = self.intercept + sum(c * x for c, x in\n                                   zip(self.coefficients, row))\n        preds.append(max(val, 0.0))\n    return preds\n</code></pre>"},{"location":"api/#model.LinearRegressionModel.predict_one","title":"<code>predict_one(x)</code>","text":"<p>Przewiduje cen\u0119 dla jednego wektora cech.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>list</code> <p>Wektor cech.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Przewidywana cena.</p> Source code in <code>model.py</code> <pre><code>def predict_one(self, x: list) -&gt; float:\n    \"\"\"\n    Przewiduje cen\u0119 dla jednego wektora cech.\n\n    Args:\n        x: Wektor cech.\n\n    Returns:\n        Przewidywana cena.\n    \"\"\"\n    return self.predict([x])[0]\n</code></pre>"},{"location":"api/#model.ModelTrainer","title":"<code>ModelTrainer</code>","text":"<p>Klasa zarz\u0105dzaj\u0105ca treningiem, ewaluacj\u0105 i zapisem modeli.</p> Source code in <code>model.py</code> <pre><code>class ModelTrainer:\n    \"\"\"Klasa zarz\u0105dzaj\u0105ca treningiem, ewaluacj\u0105 i zapisem modeli.\"\"\"\n\n    def __init__(self, input_file: str = INPUT_FILE,\n                 models_dir: str = MODELS_DIR):\n        \"\"\"\n        Args:\n            input_file: \u015acie\u017cka do przetworzonego CSV.\n            models_dir: Katalog do zapisu modeli.\n        \"\"\"\n        self.input_file = input_file\n        self.models_dir = models_dir\n        os.makedirs(models_dir, exist_ok=True)\n\n    def evaluate(self, model: object, X_test: list, y_test: list, name: str) -&gt; dict:\n        \"\"\"\n        Ewaluuje model i wypisuje metryki.\n\n        Args:\n            model: Wytrenowany model z metod\u0105 predict().\n            X_test: Testowe wektory cech.\n            y_test: Testowe warto\u015bci docelowe.\n            name: Nazwa modelu do wydruku.\n\n        Returns:\n            S\u0142ownik z metrykami: r2, rmse, mae.\n        \"\"\"\n        preds = model.predict(X_test)\n        metrics = {\n            \"r2\":   round(r2_score(y_test, preds), 4),\n            \"rmse\": round(rmse(y_test, preds), 2),\n            \"mae\":  round(mae(y_test, preds), 2),\n        }\n        print(f\"\\n\u2500\u2500 {name} \u2500\u2500\")\n        print(f\"  R\u00b2:   {metrics['r2']}\")\n        print(f\"  RMSE: {metrics['rmse']:,.0f} EUR\")\n        print(f\"  MAE:  {metrics['mae']:,.0f} EUR\")\n        return metrics\n\n    def save(self, model: object, filename: str) -&gt; None:\n        \"\"\"\n        Zapisuje model do pliku pickle.\n\n        Args:\n            model: Wytrenowany model.\n            filename: Nazwa pliku (bez \u015bcie\u017cki).\n        \"\"\"\n        path = os.path.join(self.models_dir, filename)\n        with open(path, \"wb\") as f:\n            pickle.dump(model, f)\n        print(f\"  Zapisano \u2192 {path}\")\n\n    def run(self) -&gt; None:\n        \"\"\"Uruchamia pe\u0142ny pipeline: \u0142adowanie, trening, ewaluacja, zapis.\"\"\"\n        # Dane\n        loader = DataLoader(self.input_file)\n        X, y   = loader.load()\n        X_train, X_test, y_train, y_test = loader.train_test_split(X, y)\n        print(f\"Trening: {len(X_train)} | Test: {len(X_test)}\")\n\n        # Statystyki opisowe\n        print(f\"\\nCena \u2014 \u015brednia: {mean(y):.0f} EUR | \"\n              f\"std: {std(y):.0f} | \"\n              f\"min: {min(y):.0f} | max: {max(y):.0f}\")\n\n        metrics = {}\n\n        # Regresja liniowa\n        lr = LinearRegressionModel()\n        lr.fit(X_train, y_train)\n        m_lr = self.evaluate(lr, X_test, y_test, \"Regresja liniowa\")\n        metrics[\"linear\"] = m_lr\n        self.save(lr, \"linear_model.pkl\")\n\n        # Wsp\u00f3\u0142czynniki\n        print(\"\\n  Wsp\u00f3\u0142czynniki regresji liniowej:\")\n        pairs = sorted(zip(FEATURES, lr.coefficients),\n                       key=lambda x: abs(x[1]), reverse=True)\n        for feat, coef in pairs[:10]:\n            print(f\"    {feat:30}: {coef:+.2f}\")\n\n        # Las losowy\n        try:\n            rf = RandomForestModel()\n            rf.fit(X_train, y_train)\n            m_rf = self.evaluate(rf, X_test, y_test, \"Las losowy\")\n            metrics[\"random_forest\"] = m_rf\n            self.save(rf, \"rf_model.pkl\")\n        except ImportError:\n            print(\"\\nsklearn niedost\u0119pny \u2014 pomini\u0119to las losowy.\")\n\n        # Metadane\n        meta = {\n            \"features\": FEATURES,\n            \"target\": TARGET,\n            \"n_train\": len(X_train),\n            \"n_test\": len(X_test),\n            \"price_min\": PRICE_MIN,\n            \"price_max\": PRICE_MAX,\n            \"area_min\": AREA_MIN,\n            \"area_max\": AREA_MAX,\n            \"metrics\": metrics,\n        }\n        meta_path = os.path.join(self.models_dir, \"metadata.json\")\n        with open(meta_path, \"w\", encoding=\"utf-8\") as f:\n            json.dump(meta, f, indent=2)\n        print(f\"\\nMetadane \u2192 {meta_path}\")\n</code></pre>"},{"location":"api/#model.ModelTrainer.__init__","title":"<code>__init__(input_file=INPUT_FILE, models_dir=MODELS_DIR)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>input_file</code> <code>str</code> <p>\u015acie\u017cka do przetworzonego CSV.</p> <code>INPUT_FILE</code> <code>models_dir</code> <code>str</code> <p>Katalog do zapisu modeli.</p> <code>MODELS_DIR</code> Source code in <code>model.py</code> <pre><code>def __init__(self, input_file: str = INPUT_FILE,\n             models_dir: str = MODELS_DIR):\n    \"\"\"\n    Args:\n        input_file: \u015acie\u017cka do przetworzonego CSV.\n        models_dir: Katalog do zapisu modeli.\n    \"\"\"\n    self.input_file = input_file\n    self.models_dir = models_dir\n    os.makedirs(models_dir, exist_ok=True)\n</code></pre>"},{"location":"api/#model.ModelTrainer.evaluate","title":"<code>evaluate(model, X_test, y_test, name)</code>","text":"<p>Ewaluuje model i wypisuje metryki.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>object</code> <p>Wytrenowany model z metod\u0105 predict().</p> required <code>X_test</code> <code>list</code> <p>Testowe wektory cech.</p> required <code>y_test</code> <code>list</code> <p>Testowe warto\u015bci docelowe.</p> required <code>name</code> <code>str</code> <p>Nazwa modelu do wydruku.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>S\u0142ownik z metrykami: r2, rmse, mae.</p> Source code in <code>model.py</code> <pre><code>def evaluate(self, model: object, X_test: list, y_test: list, name: str) -&gt; dict:\n    \"\"\"\n    Ewaluuje model i wypisuje metryki.\n\n    Args:\n        model: Wytrenowany model z metod\u0105 predict().\n        X_test: Testowe wektory cech.\n        y_test: Testowe warto\u015bci docelowe.\n        name: Nazwa modelu do wydruku.\n\n    Returns:\n        S\u0142ownik z metrykami: r2, rmse, mae.\n    \"\"\"\n    preds = model.predict(X_test)\n    metrics = {\n        \"r2\":   round(r2_score(y_test, preds), 4),\n        \"rmse\": round(rmse(y_test, preds), 2),\n        \"mae\":  round(mae(y_test, preds), 2),\n    }\n    print(f\"\\n\u2500\u2500 {name} \u2500\u2500\")\n    print(f\"  R\u00b2:   {metrics['r2']}\")\n    print(f\"  RMSE: {metrics['rmse']:,.0f} EUR\")\n    print(f\"  MAE:  {metrics['mae']:,.0f} EUR\")\n    return metrics\n</code></pre>"},{"location":"api/#model.ModelTrainer.run","title":"<code>run()</code>","text":"<p>Uruchamia pe\u0142ny pipeline: \u0142adowanie, trening, ewaluacja, zapis.</p> Source code in <code>model.py</code> <pre><code>def run(self) -&gt; None:\n    \"\"\"Uruchamia pe\u0142ny pipeline: \u0142adowanie, trening, ewaluacja, zapis.\"\"\"\n    # Dane\n    loader = DataLoader(self.input_file)\n    X, y   = loader.load()\n    X_train, X_test, y_train, y_test = loader.train_test_split(X, y)\n    print(f\"Trening: {len(X_train)} | Test: {len(X_test)}\")\n\n    # Statystyki opisowe\n    print(f\"\\nCena \u2014 \u015brednia: {mean(y):.0f} EUR | \"\n          f\"std: {std(y):.0f} | \"\n          f\"min: {min(y):.0f} | max: {max(y):.0f}\")\n\n    metrics = {}\n\n    # Regresja liniowa\n    lr = LinearRegressionModel()\n    lr.fit(X_train, y_train)\n    m_lr = self.evaluate(lr, X_test, y_test, \"Regresja liniowa\")\n    metrics[\"linear\"] = m_lr\n    self.save(lr, \"linear_model.pkl\")\n\n    # Wsp\u00f3\u0142czynniki\n    print(\"\\n  Wsp\u00f3\u0142czynniki regresji liniowej:\")\n    pairs = sorted(zip(FEATURES, lr.coefficients),\n                   key=lambda x: abs(x[1]), reverse=True)\n    for feat, coef in pairs[:10]:\n        print(f\"    {feat:30}: {coef:+.2f}\")\n\n    # Las losowy\n    try:\n        rf = RandomForestModel()\n        rf.fit(X_train, y_train)\n        m_rf = self.evaluate(rf, X_test, y_test, \"Las losowy\")\n        metrics[\"random_forest\"] = m_rf\n        self.save(rf, \"rf_model.pkl\")\n    except ImportError:\n        print(\"\\nsklearn niedost\u0119pny \u2014 pomini\u0119to las losowy.\")\n\n    # Metadane\n    meta = {\n        \"features\": FEATURES,\n        \"target\": TARGET,\n        \"n_train\": len(X_train),\n        \"n_test\": len(X_test),\n        \"price_min\": PRICE_MIN,\n        \"price_max\": PRICE_MAX,\n        \"area_min\": AREA_MIN,\n        \"area_max\": AREA_MAX,\n        \"metrics\": metrics,\n    }\n    meta_path = os.path.join(self.models_dir, \"metadata.json\")\n    with open(meta_path, \"w\", encoding=\"utf-8\") as f:\n        json.dump(meta, f, indent=2)\n    print(f\"\\nMetadane \u2192 {meta_path}\")\n</code></pre>"},{"location":"api/#model.ModelTrainer.save","title":"<code>save(model, filename)</code>","text":"<p>Zapisuje model do pliku pickle.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>object</code> <p>Wytrenowany model.</p> required <code>filename</code> <code>str</code> <p>Nazwa pliku (bez \u015bcie\u017cki).</p> required Source code in <code>model.py</code> <pre><code>def save(self, model: object, filename: str) -&gt; None:\n    \"\"\"\n    Zapisuje model do pliku pickle.\n\n    Args:\n        model: Wytrenowany model.\n        filename: Nazwa pliku (bez \u015bcie\u017cki).\n    \"\"\"\n    path = os.path.join(self.models_dir, filename)\n    with open(path, \"wb\") as f:\n        pickle.dump(model, f)\n    print(f\"  Zapisano \u2192 {path}\")\n</code></pre>"},{"location":"api/#model.RandomForestModel","title":"<code>RandomForestModel</code>","text":"<p>Uproszczony las losowy oparty na sklearn. U\u017cywa sklearn tylko do treningu, reszta w\u0142asna.</p> Source code in <code>model.py</code> <pre><code>class RandomForestModel:\n    \"\"\"\n    Uproszczony las losowy oparty na sklearn.\n    U\u017cywa sklearn tylko do treningu, reszta w\u0142asna.\n    \"\"\"\n\n    def __init__(self, n_estimators: int = 100, random_state: int = 42):\n        \"\"\"\n        Args:\n            n_estimators: Liczba drzew w lesie.\n            random_state: Ziarno losowo\u015bci.\n        \"\"\"\n        self.n_estimators  = n_estimators\n        self.random_state  = random_state\n        self._model        = None\n        self.feature_names = FEATURES\n\n    def fit(self, X: list, y: list) -&gt; None:\n        \"\"\"\n        Trenuje model lasu losowego.\n\n        Args:\n            X: Lista wektor\u00f3w cech.\n            y: Lista warto\u015bci docelowych.\n        \"\"\"\n        from sklearn.ensemble import RandomForestRegressor\n        self._model = RandomForestRegressor(\n            n_estimators=self.n_estimators,\n            random_state=self.random_state,\n            n_jobs=-1,\n        )\n        self._model.fit(X, y)\n\n    def predict(self, X: list) -&gt; list:\n        \"\"\"\n        Przewiduje ceny dla podanych wektor\u00f3w cech.\n\n        Args:\n            X: Lista wektor\u00f3w cech.\n\n        Returns:\n            Lista przewidywanych cen.\n        \"\"\"\n        return [max(float(p), 0.0) for p in self._model.predict(X)]\n\n    def predict_one(self, x: list) -&gt; float:\n        \"\"\"\n        Przewiduje cen\u0119 dla jednego wektora cech.\n\n        Args:\n            x: Wektor cech.\n\n        Returns:\n            Przewidywana cena.\n        \"\"\"\n        return self.predict([x])[0]\n</code></pre>"},{"location":"api/#model.RandomForestModel.__init__","title":"<code>__init__(n_estimators=100, random_state=42)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>n_estimators</code> <code>int</code> <p>Liczba drzew w lesie.</p> <code>100</code> <code>random_state</code> <code>int</code> <p>Ziarno losowo\u015bci.</p> <code>42</code> Source code in <code>model.py</code> <pre><code>def __init__(self, n_estimators: int = 100, random_state: int = 42):\n    \"\"\"\n    Args:\n        n_estimators: Liczba drzew w lesie.\n        random_state: Ziarno losowo\u015bci.\n    \"\"\"\n    self.n_estimators  = n_estimators\n    self.random_state  = random_state\n    self._model        = None\n    self.feature_names = FEATURES\n</code></pre>"},{"location":"api/#model.RandomForestModel.fit","title":"<code>fit(X, y)</code>","text":"<p>Trenuje model lasu losowego.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>list</code> <p>Lista wektor\u00f3w cech.</p> required <code>y</code> <code>list</code> <p>Lista warto\u015bci docelowych.</p> required Source code in <code>model.py</code> <pre><code>def fit(self, X: list, y: list) -&gt; None:\n    \"\"\"\n    Trenuje model lasu losowego.\n\n    Args:\n        X: Lista wektor\u00f3w cech.\n        y: Lista warto\u015bci docelowych.\n    \"\"\"\n    from sklearn.ensemble import RandomForestRegressor\n    self._model = RandomForestRegressor(\n        n_estimators=self.n_estimators,\n        random_state=self.random_state,\n        n_jobs=-1,\n    )\n    self._model.fit(X, y)\n</code></pre>"},{"location":"api/#model.RandomForestModel.predict","title":"<code>predict(X)</code>","text":"<p>Przewiduje ceny dla podanych wektor\u00f3w cech.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>list</code> <p>Lista wektor\u00f3w cech.</p> required <p>Returns:</p> Type Description <code>list</code> <p>Lista przewidywanych cen.</p> Source code in <code>model.py</code> <pre><code>def predict(self, X: list) -&gt; list:\n    \"\"\"\n    Przewiduje ceny dla podanych wektor\u00f3w cech.\n\n    Args:\n        X: Lista wektor\u00f3w cech.\n\n    Returns:\n        Lista przewidywanych cen.\n    \"\"\"\n    return [max(float(p), 0.0) for p in self._model.predict(X)]\n</code></pre>"},{"location":"api/#model.RandomForestModel.predict_one","title":"<code>predict_one(x)</code>","text":"<p>Przewiduje cen\u0119 dla jednego wektora cech.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>list</code> <p>Wektor cech.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Przewidywana cena.</p> Source code in <code>model.py</code> <pre><code>def predict_one(self, x: list) -&gt; float:\n    \"\"\"\n    Przewiduje cen\u0119 dla jednego wektora cech.\n\n    Args:\n        x: Wektor cech.\n\n    Returns:\n        Przewidywana cena.\n    \"\"\"\n    return self.predict([x])[0]\n</code></pre>"},{"location":"api/#model.mae","title":"<code>mae(y_true, y_pred)</code>","text":"<p>Oblicza \u015bredni b\u0142\u0105d bezwzgl\u0119dny (MAE).</p> Source code in <code>model.py</code> <pre><code>def mae(y_true: list, y_pred: list) -&gt; float:\n    \"\"\"Oblicza \u015bredni b\u0142\u0105d bezwzgl\u0119dny (MAE).\"\"\"\n    return mean([abs(t - p) for t, p in zip(y_true, y_pred)])\n</code></pre>"},{"location":"api/#model.mean","title":"<code>mean(values)</code>","text":"<p>Zwraca \u015bredni\u0105 arytmetyczn\u0105 listy.</p> Source code in <code>model.py</code> <pre><code>def mean(values: list) -&gt; float:\n    \"\"\"Zwraca \u015bredni\u0105 arytmetyczn\u0105 listy.\"\"\"\n    return sum(values) / len(values)\n</code></pre>"},{"location":"api/#model.pearson","title":"<code>pearson(x, y)</code>","text":"<p>Oblicza wsp\u00f3\u0142czynnik korelacji Pearsona mi\u0119dzy x i y.</p> Source code in <code>model.py</code> <pre><code>def pearson(x: list, y: list) -&gt; float:\n    \"\"\"Oblicza wsp\u00f3\u0142czynnik korelacji Pearsona mi\u0119dzy x i y.\"\"\"\n    mx, my = mean(x), mean(y)\n    num = sum((xi - mx) * (yi - my) for xi, yi in zip(x, y))\n    den = math.sqrt(sum((xi - mx) ** 2 for xi in x) *\n                    sum((yi - my) ** 2 for yi in y))\n    return num / den if den else 0.0\n</code></pre>"},{"location":"api/#model.r2_score","title":"<code>r2_score(y_true, y_pred)</code>","text":"<p>Oblicza wsp\u00f3\u0142czynnik determinacji R\u00b2.</p> Source code in <code>model.py</code> <pre><code>def r2_score(y_true: list, y_pred: list) -&gt; float:\n    \"\"\"Oblicza wsp\u00f3\u0142czynnik determinacji R\u00b2.\"\"\"\n    ss_res = sum((t - p) ** 2 for t, p in zip(y_true, y_pred))\n    ss_tot = sum((t - mean(y_true)) ** 2 for t in y_true)\n    return 1 - ss_res / ss_tot if ss_tot else 0.0\n</code></pre>"},{"location":"api/#model.rmse","title":"<code>rmse(y_true, y_pred)</code>","text":"<p>Oblicza pierwiastek b\u0142\u0119du \u015bredniokwadratowego (RMSE).</p> Source code in <code>model.py</code> <pre><code>def rmse(y_true: list, y_pred: list) -&gt; float:\n    \"\"\"Oblicza pierwiastek b\u0142\u0119du \u015bredniokwadratowego (RMSE).\"\"\"\n    return math.sqrt(mean([(t - p) ** 2 for t, p in zip(y_true, y_pred)]))\n</code></pre>"},{"location":"api/#model.std","title":"<code>std(values)</code>","text":"<p>Zwraca odchylenie standardowe listy.</p> Source code in <code>model.py</code> <pre><code>def std(values: list) -&gt; float:\n    \"\"\"Zwraca odchylenie standardowe listy.\"\"\"\n    return math.sqrt(variance(values))\n</code></pre>"},{"location":"api/#model.variance","title":"<code>variance(values)</code>","text":"<p>Zwraca wariancj\u0119 listy.</p> Source code in <code>model.py</code> <pre><code>def variance(values: list) -&gt; float:\n    \"\"\"Zwraca wariancj\u0119 listy.\"\"\"\n    m = mean(values)\n    return sum((x - m) ** 2 for x in values) / len(values)\n</code></pre>"}]}